# 本番環境デプロイ前チェックリスト

Oripaを本番環境（ConoHa VPS）で公開する際の最終確認リスト。  
デプロイ実施前に **全項目にチェック✓** を付けてください。

---

## 📋 環境変数・シークレット管理

- [ ] `server/.env` ファイルが作成されている
- [ ] すべての必須環境変数が入力されている
  - [ ] `DATABASE_HOST`, `DATABASE_PASSWORD`
  - [ ] `STRIPE_SECRET_KEY`, `STRIPE_PUBLISHABLE_KEY`
  - [ ] `RESEND_API_KEY`
  - [ ] `CLIENT_URL`, `SERVER_URL`
- [ ] `.env` ファイルが `.gitignore` に登録されている
- [ ] `.env` がGitHubにコミットされていない（確認: `git log --name-only | grep .env`）
- [ ] Stripe、Resendで本番APIキー（`live`）を使用している
  - ❌ テスト用キー（`test_`）を本番で使用していない
- [ ] データベースパスワードが十分に複雑（最低12文字、大小文字・数字・特殊文字を含む）
- [ ] 機密情報がコードにハードコードされていない

---

## 🔐 セキュリティ設定

### SSL/TLS
- [ ] Let's Encrypt で SSL証明書を取得済み
- [ ] HTTPSが有効化されている
- [ ] HTTP（ポート80）がHTTPSにリダイレクトされている
- [ ] SSL証明書の自動更新設定が有効（Certbot timer）
- [ ] Nginx の SSL プロトコルが TLSv1.2以上（TLSv1.3推奨）

### ファイアウォール
- [ ] UFW（ファイアウォール）が有効化されている
- [ ] 必要なポートのみ開放されている
  - [ ] 22 (SSH)
  - [ ] 80 (HTTP)
  - [ ] 443 (HTTPS)
  - [ ] ❌ 3000, 4200, 5432 は外部非公開
- [ ] SSH パスワード認証が無効化されている
- [ ] SSH公開鍵認証が設定されている

### アプリケーション
- [ ] CORS設定が本番ドメインに限定されている
- [ ] デバッグモード（`NODE_ENV=production`）が有効化されている
- [ ] 機密情報がエラーメッセージに含まれていない
- [ ] ロギングレベルが`info`以上（`debug`は本番NG）

---

## 📦 アプリケーションビルド・準備

### コード品質
- [ ] 最新の開発ブランチ（`develop`）がマージされている
- [ ] すべてのテストがパスしている（`npm test`）
- [ ] 不要な `console.log` がコード内に残っていない
- [ ] TypeScript のコンパイルエラーがない
- [ ] 依存パッケージが最新セキュリティパッチを適用している

### ビルド確認
- [ ] クライアント（Angular）がビルド成功（`npm run build`）
- [ ] サーバー（Node.js）がコンパイル成功（`tsc`）
- [ ] Docker イメージがビルド成功（`docker compose build`）

### データベース
- [ ] マイグレーションスクリプトが準備されている
- [ ] 初期データ（シード）がある場合は設定済み
- [ ] データベース接続テストが成功している

---

## 🚀 デプロイ前確認

### インフラストラクチャ
- [ ] ConoHa VPS が起動している
- [ ] Ubuntu/Debian OS が最新にアップデートされている
- [ ] Docker・Docker Compose が正常にインストールされている
- [ ] Nginx がインストール済み
- [ ] ディスク容量が十分（`df -h` で確認）
- [ ] メモリが2GB以上ある

### ドメイン
- [ ] ドメインを取得済み
- [ ] DNS A レコードが VPS IP に向いている
  - [ ] `yourdomain.com`
  - [ ] `api.yourdomain.com`
- [ ] DNS 伝播が完了している（`nslookup yourdomain.com`）

### リポジトリ
- [ ] GitHub リポジトリへのアクセス権がある
- [ ] デプロイ用の SSH キーが VPS に設定済み
- [ ] リポジトリが本番用コードで最新化されている

---

## 🔍 デプロイ手順実施後の確認

### コンテナ起動
- [ ] `docker compose ps` ですべてのコンテナが `Up` 状態
- [ ] `docker compose logs` でエラーが出ていない

### 動作確認
- [ ] ブラウザで `https://yourdomain.com` にアクセス可能
- [ ] フロントエンドが正常に表示される
- [ ] ブラウザコンソールにエラーがない
- [ ] `https://api.yourdomain.com/api/...` にアクセス可能
- [ ] API レスポンスが正常に返される

### 機能テスト
- [ ] ログイン・ログアウトが動作
- [ ] ユーザー登録が動作
- [ ] Stripe 決済機能が動作
- [ ] メール送信（Resend）が動作
- [ ] データベースへのアクセスが正常

### パフォーマンス・ログ
- [ ] ページロード時間が妥当（3秒以内が目安）
- [ ] Nginx アクセスログが記録されている
- [ ] エラーログに異常がない
- [ ] CPU・メモリ使用率が安定している（`docker stats`）

---

## 🛡️ セキュリティテスト

- [ ] SSL/TLS レーティングが A以上（SSL Labs: https://www.ssllabs.com/ssltest/）
- [ ] HTTP → HTTPS リダイレクトが機能している
- [ ] HSTS（Strict-Transport-Security）ヘッダが有効
- [ ] 機密情報がレスポンスヘッダに含まれていない
- [ ] XSS対策（X-Content-Type-Options, CSP）が有効
- [ ] CSRF トークンが実装されている（必要な場合）

---

## 📊 監視・バックアップ

### バックアップ
- [ ] 初回バックアップが正常に完了
  ```bash
  docker compose exec db pg_dump -U user oripa > backup_initial.sql
  ```
- [ ] バックアップスクリプトが cron に登録済み
- [ ] バックアップファイルが安全に保管されている

### 監視
- [ ] サーバーの生存確認監視を設定（オプション）
- [ ] ディスク容量アラートを設定（オプション）
- [ ] エラーログ監視を設定（オプション）

---

## 📝 ドキュメント

- [ ] チーム全員が「ConoHa_VPS_デプロイ手順書.md」を読んでいる
- [ ] 緊急時の対応手順が共有されている
- [ ] ログイン認証情報（VPS IP、SSH キーなど）が安全に保管されている
- [ ] デプロイ日時とデプロイ者名が記録されている

---

## 🎯 最終確認

| 項目 | 確認内容 | OK/NG |
|---|---|---|
| **セキュリティ** | APIキーが環境変数管理、SSHパスワード認証無効 | ☐ |
| **可用性** | コンテナが自動再起動、SSL証明書自動更新 | ☐ |
| **パフォーマンス** | メモリ2GB以上、CPU使用率<50% | ☐ |
| **機能** | 全主要機能テスト完了 | ☐ |
| **監視** | ログ記録、バックアップ設定済み | ☐ |

---

## ✅ デプロイ完了！

**デプロイ実施日時**: \_\_\_\_年\_\_\_月\_\_\_日 \_\_\_\_時\_\_\_\_分  
**デプロイ実施者**: \_\_\_\_\_\_\_\_\_\_\_\_\_\_\_  
**本番URL**: https://yourdomain.com  

問題発生時の連絡先：  
\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_

---

**参考**: 何か問題が発生した場合は、「ConoHa_VPS_デプロイ手順書.md」の「トラブルシューティング」セクションを参照してください。
