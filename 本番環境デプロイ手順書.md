# 本番環境デプロイ手順書 (Stripe決済対応)

Oripa を Stripe 決済システム対応で本番環境（ConoHa VPS）で公開するための完全手順。

**前提**: Stripe 本番環境では **HTTPS（SSL）が必須** です。このガイドは SSL 取得を初回から含めています。

---

## 概要フロー

```
1. ConoHa VPS 新規購入・セットアップ
   ↓
2. Docker・リポジトリクローン・環境変数設定
   ↓
3. ドメイン DNS A レコード設定
   ↓
4. Let's Encrypt SSL 証明書取得（DNS 伝播後）
   ↓
5. Nginx リバースプロキシ・SSL 設定
   ↓
6. アプリケーション起動・動作確認
   ↓
7. Stripe 本番接続テスト
```

---

## 第1段階：ConoHa VPS 新規購入・初期セットアップ

### 1-1. VPS 購入

[ConoHa ダッシュボード](https://cp.conoha.jp/VPS/Dashboard)で新規 VPS を購入：

- **OS**: Ubuntu 24.04 LTS
- **プラン**: 2GB RAM 以上推奨
- **ホスト名**: メモしておく

### 1-2. 初期設定

```bash
ssh root@<VPS_IP>

# システムアップデート
apt update && apt upgrade -y

# 新しいユーザー作成
adduser oripa
usermod -aG sudo oripa

# ファイアウォール設定
ufw default deny incoming
ufw default allow outgoing
ufw allow 22/tcp
ufw allow 80/tcp
ufw allow 443/tcp
ufw enable
```

---

## 第2段階：Docker セットアップ・アプリケーション配置

### 2-1. Docker インストール

```bash
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh
docker --version
docker compose version
```

### 2-2. リポジトリクローン・環境変数設定

```bash
cd /root
git clone https://github.com/konishiprog/oripa.git
cd oripa
git checkout develop
```

### 2-3. 本番環境用 `.env` 作成

```bash
cat > server/.env << 'EOF'
# Database
DB_HOST=db
DB_PORT=5432
DB_USER=user
DB_PASS=<強力なパスワード>
DB_NAME=oripa
DATABASE_HOST=db
DATABASE_PORT=5432
DATABASE_NAME=oripa
DATABASE_USER=user
DATABASE_PASSWORD=<強力なパスワード>
DATABASE_URL=postgresql://user:<強力なパスワード>@db:5432/oripa

# URLs（本番ドメイン）
CLIENT_URL=https://yourdomain.com
SERVER_URL=https://api.yourdomain.com
ALLOWED_ORIGINS=https://yourdomain.com

# Stripe 本番キー
STRIPE_SECRET_KEY=sk_live_xxxxxxxxxxxx
STRIPE_PUBLISHABLE_KEY=pk_live_xxxxxxxxxxxx
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxx

# Resend
RESEND_API_KEY=re_xxxxxxxxxxxx
EMAIL_FROM=support@yourdomain.com

# Admin
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=<強力なパスワード>

# Server
NODE_ENV=production
PORT=3000
LOG_LEVEL=info
EOF
```

⚠️ **強力なパスワードと実際の Stripe キーに置き換えてください**

### 2-4. Docker Compose で起動

```bash
# docker-compose.yml のパスワードも一致させる
sed -i 's/POSTGRES_PASSWORD: pass/POSTGRES_PASSWORD: <強力なパスワード>/g' docker-compose.yml

# 起動
docker compose up -d

# 確認
docker compose ps
```

---

## 第3段階：ドメイン DNS 設定（重要）

### 3-1. ムームードメイン DNS 設定

1. ムームードメイン管理画面 → ムームーDNS
2. DNS レコード追加：
   - **A レコード（@）**: 値 = `<VPS_IP>`
   - **A レコード（api）**: 値 = `<VPS_IP>`

### 3-2. DNS 伝播確認

```bash
nslookup yourdomain.com
# <VPS_IP> が返ってくるまで待つ（最大24時間）
```

**この確認が取れるまで、次のステップに進まないでください。**

---

## 第4段階：SSL 証明書取得（Let's Encrypt）

⚠️ **DNS が確認できてから実行してください**

### 4-1. 初期 Nginx セットアップ

```bash
apt install -y nginx
systemctl start nginx
```

### 4-2. SSL 証明書取得

```bash
apt install -y certbot python3-certbot-nginx

# Nginx を一時停止
systemctl stop nginx

# 証明書取得
certbot certonly --standalone -d yourdomain.com -d api.yourdomain.com \
  --non-interactive --agree-tos -m <your-email@example.com>

# Nginx を再起動
systemctl start nginx
```

**成功メッセージが表示されたら OK です。**

---

## 第5段階：Nginx リバースプロキシ設定

### 5-1. Nginx 設定ファイル作成

```bash
sudo tee /etc/nginx/sites-available/oripa > /dev/null << 'EOF'
# HTTP → HTTPS リダイレクト
server {
    listen 80;
    listen [::]:80;
    server_name yourdomain.com api.yourdomain.com;
    return 301 https://$server_name$request_uri;
}

# フロントエンド (yourdomain.com)
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name yourdomain.com;

    ssl_certificate /etc/letsencrypt/live/yourdomain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/yourdomain.com/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    location / {
        proxy_pass http://localhost:4200;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# バックエンド API (api.yourdomain.com)
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name api.yourdomain.com;

    ssl_certificate /etc/letsencrypt/live/yourdomain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/yourdomain.com/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
EOF
```

### 5-2. Nginx 有効化

```bash
sudo ln -s /etc/nginx/sites-available/oripa /etc/nginx/sites-enabled/oripa
sudo rm /etc/nginx/sites-enabled/default
sudo nginx -t
sudo systemctl reload nginx
```

---

## 第6段階：動作確認・Stripe テスト

### 6-1. ブラウザでアクセス

```
https://yourdomain.com          # フロントエンド
https://api.yourdomain.com      # バックエンド API
```

### 6-2. Stripe 本番決済テスト

1. Stripe ダッシュボードで本番キーを使用確認
2. テスト決済を実行
3. Stripe ウェブフック設定確認

### 6-3. ログ確認

```bash
docker compose logs -f server
sudo tail -f /var/log/nginx/access.log
```

---

## 第7段階：運用・保守設定

### 7-1. SSL 自動更新

```bash
sudo systemctl enable certbot.timer
sudo systemctl status certbot.timer
```

### 7-2. バックアップ

```bash
# 日次バックアップスクリプト
cat > /usr/local/bin/backup-oripa.sh << 'EOF'
#!/bin/bash
BACKUP_DIR="/backups"
mkdir -p $BACKUP_DIR
docker compose -f /root/oripa/docker-compose.yml exec -T db pg_dump -U user oripa > $BACKUP_DIR/backup_$(date +%Y%m%d_%H%M%S).sql
EOF

chmod +x /usr/local/bin/backup-oripa.sh

# crontab で毎日実行
(crontab -l 2>/dev/null; echo "0 2 * * * /usr/local/bin/backup-oripa.sh") | crontab -
```

### 7-3. モニタリング

```bash
# 定期的に確認
docker stats
df -h
docker compose ps
```

---

## チェックリスト

- [ ] VPS 購入・初期設定完了
- [ ] Docker 起動・コンテナ全て Up 状態
- [ ] ドメイン DNS A レコード設定（@ と api）
- [ ] DNS 伝播確認（nslookup で VPS IP 返却）
- [ ] SSL 証明書取得成功
- [ ] Nginx 設定ファイル作成・有効化
- [ ] HTTPS でアクセス可能（自動 HTTP→HTTPS リダイレクト）
- [ ] Stripe 本番キーで決済テスト成功
- [ ] ログ確認（エラーなし）
- [ ] SSL 自動更新設定
- [ ] バックアップ設定

---

## トラブルシューティング

| 問題 | 原因 | 解決策 |
|---|---|---|
| DNS が反映されない | 設定後間もない | 24時間待つ、`nslookup` で確認 |
| SSL 証補取得失敗 | DNS が未伝播 | DNS 確認後に再度実行 |
| HTTPS アクセス失敗 | Nginx 設定エラー | `nginx -t` で確認、ログ確認 |
| Stripe 決済エラー | API キー間違い | Stripe ダッシュボードで本番キー確認 |

---

**最終更新**: 2026年6月14日

